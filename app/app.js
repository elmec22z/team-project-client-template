/* Empty for now. :) */
import React from 'react';
import ReactDOM from 'react-dom'
import {IndexRouter, Router, Route, browserHistory } from 'react-router'

ReactDOM.render((
	<Router history={browserHistory}>
		<Route path="/" component={App}>
			

))